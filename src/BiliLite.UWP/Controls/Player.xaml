<UserControl
    x:Class="BiliLite.Controls.Player"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BiliLite.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shakaPlayer="using:BiliLite.Player.ShakaPlayer"

    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid Visibility="{x:Bind ShowMediaPlayer,Mode=OneWay}">
            <!--播放器-->
            <MediaPlayerElement x:Name="mediaPlayerVideo" ></MediaPlayerElement>
            <!--音视频分离-->
            <MediaPlayerElement x:Name="mediaPlayerAudio"  Visibility="Collapsed"></MediaPlayerElement>
            <!--<vlc:VideoView x:Name="vlcVideoView" Initialized="vlcVideoView_Initialized"></vlc:VideoView>-->
        </Grid>
        <Grid Visibility="{x:Bind ShowShakaPlayer,Mode=OneWay}">
            <shakaPlayer:ShakaPlayerControl x:Name="ShakaPlayer" 
                                            PlayerLoaded="ShakaPlayer_OnPlayerLoaded"
                                            Ended="ShakaPlayer_OnEnded"
                                            PositionChanged="ShakaPlayer_OnPositionChanged"></shakaPlayer:ShakaPlayerControl>
        </Grid>
    </Grid>
</UserControl>
