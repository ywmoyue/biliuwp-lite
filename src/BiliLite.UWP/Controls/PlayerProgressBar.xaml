<UserControl
    x:Class="BiliLite.Controls.PlayerProgressBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BiliLite.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Name="TrackGrid" Background="Transparent" Height="21" VerticalAlignment="Center"
          PointerMoved="TrackGrid_PointerMoved" PointerPressed="TrackGrid_PointerPressed">
        <ToolTipService.ToolTip>
            <ToolTip Name="ProgressToolTip" />
        </ToolTipService.ToolTip>

        <!-- 可视背景条（5px 高度，位于中间） -->
        <Rectangle Fill="LightGray" 
                   Height="5" 
                   VerticalAlignment="Center" />

        <!-- 进度条填充（同样保持在中间 5px 高度） -->
        <Rectangle Name="ProgressRectangle"  
                   Fill="{ThemeResource SystemAccentColor}"  
                   Height="5" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Left" />

        <!-- Thumb 容器及组件的垂直居中处理 -->
        <Canvas VerticalAlignment="Center" Height="20">
            <Thumb Name="ProgressThumb" 
                   DragDelta="ProgressThumb_DragDelta" 
                   DragCompleted="ProgressThumb_DragCompleted" 
                   Width="20" Height="20" 
                   VerticalAlignment="Center">
                <Thumb.RenderTransform>
                    <TranslateTransform Y="-1" />
                </Thumb.RenderTransform>
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Fill="{ThemeResource SystemAccentColor}" 
                                 Width="20" Height="20" />
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
        </Canvas>
    </Grid>

</UserControl>
